spring.application.name=demo

# 1. Connexion à MySQL (L'adresse de la base que tu viens de créer)
spring.datasource.url=jdbc:mysql://localhost:3306/optimal_db?useSSL=false&serverTimezone=UTC

# 2. Tes identifiants MySQL
spring.datasource.username=root
spring.datasource.password=Amos66642122

# 3. Le pilote (Le traducteur entre Java et MySQL)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 4. Magie Hibernate : Crée les tables automatiquement à partir du code Java
spring.jpa.hibernate.ddl-auto=update

# 5. Affiche les requêtes SQL dans la console IntelliJ (très utile pour débugger)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration